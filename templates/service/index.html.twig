{% extends 'base.html.twig' %}

{% block title %}Services{% endblock %}

{% block body %}
    <div class="wrapper bgded overlay" style="background-image:url({{ asset('images/imagemiss.jpg') }});">
        <div class="page-title text-center wow fadeInRightBig"> <h1 class="heading">Services</h1></div>
    </div>
    <div class="row pt-20" style="background-color: #f9f9f9">
        <div class="col-xl-8 offset-xl-2">
            <div class="sec-title text-center black wow bounceIn">
                <span>EDITION 2022</span>
                <h3>Services</h3>
                <p>SERVICES MISS-SHINE ENTREPREUNARIAT.</p>
            </div><!-- /.sec-title -->
        </div>
    </div>
    <div class="brand-carousel-wrapper light-blue-bg  wow fadeIn">
        <div class="container">
            <div class="brand-carousel owl-carousel owl-theme">
                {% for partenaire in partenaires %}
                    {% if partenaire.logo %}
                        <div class="item">
                            <img src="{{ asset('uploads/logo/' ~ partenaire.logo) }}">
                        </div>
                    {% else %}
                        <div class="item">
                            <img src="{{ asset('images/brand-1.png') }}">
                        </div>
                    {% endif %}
                {% endfor %}
            </div><!-- /.brand-carousel -->
        </div><!-- /.thm-container -->
    </div>
{% endblock %}
