{% extends 'baseback.html.twig' %}

{% block title %}Partenaire index{% endblock %}

{% block body %}
    <div class="content">
        <div class="card card-stats">
            <div class="card-header">
                <a class="btn btn-dark" href="{{ path('partenaire_new') }}">Create new</a>
            </div>
            <div class="card-body ">

           {#     <table class="table">
                    <thead>
                    <tr>
                        <th>Id</th>
                        <th>Logo</th>
                        <th>Name</th>
                        <th>Active</th>
                        <th>actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for partenaire in partenaires %}
                        <tr>
                            <td>{{ partenaire.id }}</td>
                            <td>{{ partenaire.logo }}</td>
                            <td>{{ partenaire.name }}</td>
                            <td>{{ partenaire.active ? 'Yes' : 'No' }}</td>
                            <td>
                                <a href="{{ path('partenaire_show', {'id': partenaire.id}) }}">show</a>
                                <a href="{{ path('partenaire_edit', {'id': partenaire.id}) }}">edit</a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="5">no records found</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>#}
                <div id="partenaires">{{ 'loading'|trans({}, 'messages') }}</div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function () {
            $('#partenaires').initDataTables({{ datatable_settings(datatable) }});
        });
    </script>
{% endblock %}
